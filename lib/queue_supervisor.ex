defmodule DistrLogger.Queue.Supervisor do
  # use DynamicSupervisor
  # @self __MODULE__
  #
  # # API
  # def start_child(working_directory, logger_hub, max_queue_length, flush_chunk_size) do
  #   @self |> DynamicSupervisor.start_child({DistrLogger.Queue, [working_directory, logger_hub, max_queue_length, flush_chunk_size]})
  # end

end
